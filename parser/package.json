{
  "scripts": {
    "build": "del-cli --force dist && tsc && node dist/build.js",
    "fix-meta": "del-cli --force dist && tsc && node dist/fix-meta.js",
    "shuffle": "del-cli --force dist && tsc && node dist/shuffle.js",
    "union": "del-cli --force dist && tsc && node dist/union.js",
    "check": "del-cli --force dist && tsc && node dist/check.js",
    "evolution": "del-cli --force dist && tsc && node dist/evolution.js"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "fs": "^0.0.1-security"
  },
  "devDependencies": {
    "@types/node": "^20.2.1",
    "typescript": "^5.0.4"
  }
}
